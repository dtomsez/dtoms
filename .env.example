# คัดลอกไฟล์นี้เป็น .env.local แล้วใส่ค่าจากโปรเจกต์ Supabase ของคุณ
# หาได้ที่ Supabase Dashboard → Project Settings → Data API / API Keys
#
# ถ้าไม่ตั้งค่าสองบรรทัดนี้ แอปจะทำงานในโหมด Guest (เก็บข้อมูลใน localStorage ของเบราว์เซอร์)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
